import { TodoIsDoneDirective } from "./todo-isdone.directive";

describe("TodoisDoneDirective", () => {
  it("should create an instance", () => {
    const directive = new TodoIsDoneDirective();
    expect(directive).toBeTruthy();
  });
});
